
TCP의 경우 연속되는 byte가 흐르듯이 데이터를 전송하기 때문에
send() 했던 크기 만큼 그대로 recv()한다는 보장이 없다.
다시말해, 한 번에 데이터를 모두 받아오지 않을 수 있기 때문에,
전체 바이트를 다 읽을 때까지 반복하는 코드가 들어가야 한다.

하지만 server2.cpp에서 만든 서버에는 recv()로 
데이터를 받아올 때 반복하지 않으므로
필요한 바이트를 다 읽어오지 못할 수 있는 문제가 발생된다.

